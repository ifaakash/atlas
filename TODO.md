# Atlas Editor — TODO

## High Priority
- [ ] Tab character rendering (expand \t to spaces on display, configurable tab width)
- [ ] Search & Replace (Ctrl+H) — find and replace with confirm each / replace all
- [ ] Better UI — welcome screen, color theme, cleaner status bar
- [ ] Bracket matching — highlight matching () {} [] when cursor is on one

## Medium Priority
- [ ] Configuration file (~/.atlasrc) — tab width, theme, keybindings
- [ ] Multiple file buffers — open/switch between files (Ctrl+N, Ctrl+Tab)
- [ ] Word wrap / soft wrap — long lines wrap visually
- [ ] More syntax languages — Python, JavaScript, Markdown, shell, JSON, YAML
- [ ] Ctrl+Arrow word-level cursor movement (jump between words)
- [ ] Delete entire line shortcut (Ctrl+K or Ctrl+Shift+K)
- [ ] Undo grouping — typing "hello" = 1 undo step, not 5

## Low Priority
- [ ] File browser / tree view sidebar
- [ ] Split panes — horizontal/vertical splits
- [ ] Auto-closing brackets — type ( and ) appears automatically
- [ ] Minimap scrollbar (like VS Code)
- [ ] Session restore — remember last open file and cursor position
- [ ] Plugin system — loadable .so modules
- [ ] Performance — lazy syntax highlighting, viewport-only rendering for huge files
